import LoginPage from "./pages/LoginPage";

export default function App() {
  // Futuramente aqui vamos colocar rotas, autenticação, etc.
  // Por enquanto só mostramos a tela de login
  return <LoginPage />;
}
