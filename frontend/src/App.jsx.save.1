g-slate-400 rounded-full mb-12" />
        <div className="flex flex-col gap-6">
          <div className="h-4 bg-slate-300 rounded-full w-full" />
          <div className="h-4 bg-slate-300 rounded-full w-3/4" />
        </div>
      </aside>

      {/* Conteúdo - Direita */}
      <main className="flex-1 flex flex-col gap-6">
        
        {/* Header */}
        <header className="h-24 bg-egm-card rounded-egm px-10 flex items-center justify-between">
          <h1 className="text-xl font-bold opacity-70">Visão Geral</h1>
          <div className="w-10 h-10 bg-slate-400 rounded-full" />
        </header>

        {/* Bento Grid Central */}
        <div className="flex-1 flex gap-6">
          {/* Main Card */}
          <section className="flex-[3] bg-egm-card rounded-egm p-8">
            <div className="w-full h-full border-4 border-dashed border-slate-300 rounded-[2rem]" />
          </section>
          
          {/* Side Cards */}
          <div className="flex-1 flex flex-col gap-6">
            <div className="h-1/3 bg-egm-card rounded-egm" />
            <div className="flex-1 bg-egm-card rounded-egm" />
 
